CREATE TABLE VMCpuAlerts (
            ID int IDENTITY(1,1) PRIMARY KEY,
            VMName nvarchar(255) NOT NULL,
            CpuPercentage decimal(5,2),
            vCenterName nvarchar(255),
            LastEmailSent datetime,

        )
