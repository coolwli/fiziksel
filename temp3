$content = $content -replace "@\{", "{"
$content = $content -replace "\}", "}"
$content = $content -replace "=\(", ":["
$content = $content -replace "\)", "]"
$content = $content -replace "=", ":"

# PowerShell nesnesine dönüştür
$data = Invoke-Expression $content
