                const osCount = {}; 
                filteredData.forEach(row => {
                    const osCell=row[Object.keys(row)[10]];
                    const osValue = osCell.trim(); 

                    if (osCount[osValue]) {
                        osCount[osValue]++;
                    } else {
                        osCount[osValue] = 1;
                    }
                });
                const table = document.getElementById("os-table");
                osCount.forEach(os => {
                    add to table os name and count 

                });
