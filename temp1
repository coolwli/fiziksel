<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Üst Header Menü</title>
    <style>
        /* Genel stil ayarları */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }

        /* Header stil ayarları */
        .header {
            background-color: #333;
            padding: 10px 0;
        }

        /* Menü stili ayarları */
        .nav {
            display: flex;
            justify-content: flex-start;
            /* Menü öğelerini sola yasla */
            align-items: center;
            padding-left: 20px;
            /* Header'ın solundan biraz boşluk bırak */
        }

        .nav-item {
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            position: relative;
            display: inline-block;
            font-size: 18px;
            /* Font boyutunu büyüt */
        }

        .nav-item:not(:last-child)::after {
            content: '';
            position: absolute;
            top: 50%;
            right: -1px;
            /* Çizgi genişliği */
            height: 20px;
            width: 1px;
            /* Çizgi genişliği */
            background-color: white;
            transform: translateY(-50%);
        }
    </style>
</head>

<body>
    <header class="header">
        <nav class="nav">
            <a href="#" class="nav-item">Menü 1</a>
            <a href="#" class="nav-item">Menü 2</a>
            <a href="#" class="nav-item">Menü 3</a>
        </nav>
    </header>
</body>

</html>
