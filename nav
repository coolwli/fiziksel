using DocumentFormat.OpenXml.Math;
using OfficeOpenXml;
using OfficeOpenXml.Drawing;
using OfficeOpenXml.Drawing.Chart;
using System;
using System.IO;
using System.Web.UI;

namespace WebApplication1
{
    public partial class _default : System.Web.UI.Page
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            // Assuming you want to call the function when the page loads
            if (!IsPostBack)
            {
                // Add elements to the array and call the JavaScript function
                string script = @"<script>printArray('element1');</script>"; // Add more elements if needed
                ClientScript.RegisterStartupScript(this.GetType(), "CallMyFunction", script);
            }
        }




    }
}
        < script >
            var myArray = ["s"];

function printArray(asm)
{
    myArray.push(asm);
    console.log(myArray[1]);
}
        </ script >
